name: funkin_editors
description: "Friday Night Funkin' modding tools, built for a phone."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  just_audio: ^0.10.0
  provider: ^6.1.2
  shared_preferences: ^2.3.0
  xml: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
